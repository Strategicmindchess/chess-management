import { prisma } from '../src/lib/prisma'; async function main() { const inst = await prisma.classInstance.findFirst({ where: { lectureName: 'Lecture 3: Lecture 3: Knight, Pawn, Notation' } }); if (inst) { console.log('Found Lecture 3!'); console.log('Date in DB:', inst.date.toISOString()); console.log('Start Time:', inst.startTime); console.log('End Time:', inst.endTime); } else { console.log('Not found'); } } main().catch(console.error).finally(() => prisma.$disconnect());
