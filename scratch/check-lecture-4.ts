import { prisma } from '../src/lib/prisma'; async function main() { const insts = await prisma.classInstance.findMany({ orderBy: { updatedAt: 'desc' }, take: 3 }); for (const inst of insts) { console.log('ID:', inst.id, '| LectureName:', inst.lectureName, '| Date:', inst.date.toISOString(), '| Start:', inst.startTime, '| End:', inst.endTime, '| UpdatedAt:', inst.updatedAt.toISOString()); } } main().catch(console.error).finally(() => prisma.$disconnect());
