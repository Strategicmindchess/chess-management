import { prisma } from '../src/lib/prisma'; async function main() { const reqs = await prisma.coachRescheduleRequest.findMany({ where: { status: 'APPROVED' }, orderBy: { reviewedAt: 'desc' }, take: 5, include: { classInstance: { include: { batch: true } } } }); for (const r of reqs) { console.log('Reschedule ID:', r.id, '| DB Date:', r.classInstance.date.toISOString(), '| Time:', r.classInstance.startTime); } } main().catch(console.error).finally(() => prisma.$disconnect());
